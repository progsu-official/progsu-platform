School logos for the dashboard Education card.

Filename must be the school name slugified, as `<slug>.png`:
  "Georgia State University" -> georgia-state-university.png

Square art works best (rendered 48x48, object-cover). Any school without a
file here falls back to a monogram tile automatically — see
app/dashboard/school-logo.tsx.
